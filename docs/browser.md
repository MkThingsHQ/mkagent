# Browser

The Browser pane is available from the desktop chat surface and session tools. Instances can be bound to a session, navigated, focused, inspected, and closed through the shared Browser manager. `web_search` and `web_fetch` remain available for non-interactive retrieval.

Browser tools obey session permissions. The WebUI delegates browser capabilities to a connected host and does not turn remote workspaces into a product feature.
